"""Audit that codebase respects config contracts.

Required checks (from contracts pack):
- No os.getenv usage outside settings.
- No deprecated env keys.
- No raw SQL referencing unknown tables (optional).

This is a stub scaffold.
"""

if __name__ == "__main__":
    raise SystemExit("TODO: implement audit checks")
